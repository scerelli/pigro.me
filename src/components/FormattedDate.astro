---
interface Props {
	date: Date;
	showYear?: Boolean;
}

const { date, showYear } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('it-it', {
			year: showYear ? 'numeric' : undefined,
			month: 'short',
			day: 'numeric',
		})
	}
</time>
